import Link from "next/link";import {documents} from "../../data";
export default function ReviewDetail({params}:{params:{id:string}}){const d=documents.find(x=>x.id===params.id)||documents[0];return <main className="wrap"><div className="heading"><div><div className="eyebrow">Forensic inspection / {d.id}</div><h1>{d.name}</h1></div><Link href="/review" className="button secondary">← Queue</Link></div><div className="detail-grid"><section className="card panel"><div className="preview"/><div className="section-head" style={{marginTop:20,marginBottom:0}}><div><h3>{d.verdict}</h3><p className="muted" style={{fontSize:12}}>Visual evidence / ELA analysis</p></div><div className="score" style={{fontSize:39,color:d.tone==="green"?"var(--green)":"var(--red)"}}>{d.score}<small style={{display:"block",font:'11px DM Sans',color:'var(--muted)'}}>risk score</small></div></div></section><section className="card panel"><span className={`badge ${d.tone}`}>{d.verdict}</span><h2 style={{marginTop:16}}>Signal summary</h2><p className="muted" style={{fontSize:14,lineHeight:1.6}}>The combined reading prioritises this document for human review. Compare the visual evidence with the semantic result before recording a decision.</p><div className="detail-list"><div><span>ELA score</span><b>{d.score>50?"88.2":"5.2"}</b></div><div><span>Metadata</span><b>{d.score>50?"Tampered":"Clear"}</b></div><div><span>Semantic audit</span><b>{d.score>50?"Discrepancy":"Passed"}</b></div><div><span>Processing time</span><b>1.2 sec</b></div></div><div className="actions"><button className="button primary">Mark verified</button><button className="button secondary">Request review</button></div></section></div></main>}
