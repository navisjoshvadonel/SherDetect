"use client";
import Link from "next/link";
export function VerdictModal({ verdict, score, onClose }: { verdict:"verified"|"forgery"; score:number; onClose:()=>void }) { const good=verdict==="verified"; return <div className="modal-backdrop" role="dialog" aria-modal="true"><div className={`verdict-modal ${good?"modal-green":"modal-red"}`}><button className="close" onClick={onClose} aria-label="Close result">×</button><div className="verdict-icon">{good?"✓":"!"}</div><div className="eyebrow">Forensic audit complete</div><h2>{good?"Verified authentic":"Forgery detected"}</h2><p>{good?"The document passed the available integrity checks.":"Multiple signals require your attention before approval."}</p><div className="modal-score"><strong>{score}</strong><span>/ 100<br/>risk score</span></div><div className="modal-actions"><button className="button secondary" onClick={onClose}>Close result</button><Link className="button primary" href="/review">Open review queue ↗</Link></div></div></div>; }
