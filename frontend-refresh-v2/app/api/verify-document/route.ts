import { NextRequest, NextResponse } from "next/server";
export async function POST(req: NextRequest){ const form=await req.formData(); const file=form.get("file"); if(!(file instanceof File)) return NextResponse.json({error:"No file provided"},{status:400}); for(const base of [process.env.BACKEND_URL||"http://localhost:8001",process.env.AI_ENGINE_URL||"http://localhost:8000"]){ try { const body=new FormData(); body.append("file",file,file.name); const res=await fetch(`${base}/api/verify-document`,{method:"POST",body}); if(res.ok)return NextResponse.json(await res.json()); } catch {} } return NextResponse.json({error:"Forensic backend is offline. Start the service on port 8001."},{status:503}); }
