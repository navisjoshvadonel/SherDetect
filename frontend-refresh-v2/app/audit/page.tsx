import {documents} from "../data";
export default function Audit(){return <main className="wrap"><div className="heading"><div><div className="eyebrow">Audit trail</div><h1>A record you can return to.</h1></div><p>Submission and forensic states in a simple, readable table.</p></div><div className="card table"><div className="table-head"><span>Event</span><span>Document</span><span>Risk score</span><span>State</span><span>Time</span></div>{documents.map((d,i)=><div className="row" key={d.id}><div><span className="doc">{i?"Document submitted":"Forensic report generated"}</span><span className="id">{d.id}</span></div><span>{d.name}</span><span className="score">{d.score}</span><span className={`badge ${d.tone}`}>{d.verdict}</span><span>{d.date}</span></div>)}</div></main>}
