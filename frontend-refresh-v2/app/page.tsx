import Link from "next/link";
import { documents } from "./data";
export default function Home(){return <main className="wrap"><section className="hero"><div className="hero-copy"><div className="eyebrow">Document integrity, made clear</div><h1>Know what changed before it matters.</h1><p className="lead">A composed workspace for pixel forensics, metadata signals and semantic review. Evidence first, decisions second.</p><div className="actions"><Link href="/submit" className="button primary">Start an audit ↗</Link><Link href="/review" className="button secondary">Open review queue</Link></div></div><div className="hero-visual"><div className="photo"/><div className="float-card reading"><span className="muted">Latest risk reading</span><strong>91</strong><div className="bar"><i/></div><span className="muted" style={{fontSize:11}}>Forgery detected</span></div><div className="float-card pulse-card"><div className="pulse"/><div><b>Forensics active</b><div className="muted">6 signals checked</div></div></div></div></section><section className="section"><div className="section-head"><div><div className="eyebrow">Workspace pulse</div><h2>The signal, at a glance.</h2></div><p>28 August 2026</p></div><div className="metrics"><div className="card metric"><div className="eyebrow">Reviewed</div><strong>248</strong><small>Across six domains</small></div><div className="card metric"><div className="eyebrow">Needs attention</div><strong>07</strong><small>Ranked by risk</small></div><div className="card metric"><div className="eyebrow">Average turnaround</div><strong>1.8s</strong><small>Upload to report</small></div></div></section><section className="section"><div className="section-head"><div><div className="eyebrow">Recent audits</div><h2>Keep the trail close.</h2></div><Link href="/documents" className="button secondary">View documents</Link></div><div className="card table">{documents.map(d=><div className="row" key={d.id}><div><span className="doc">{d.name}</span><span className="id">{d.id} · {d.domain}</span></div><span>{d.kind}</span><span className="score">{d.score}</span><span className={`badge ${d.tone}`}>{d.verdict}</span><Link href={`/review/${d.id}`} className="button secondary">Inspect</Link></div>)}</div></section></main>}
