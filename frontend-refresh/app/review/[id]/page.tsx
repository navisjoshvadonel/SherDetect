import Link from "next/link";
import { documents } from "../../data";
export default function DetailPage({params}:{params:{id:string}}){const doc=documents.find(item=>item.id===params.id)||documents[0]; return <><div className="page-heading"><div><div className="eyebrow">Forensic inspection / {doc.id}</div><h1>{doc.name}</h1></div><Link className="button secondary" href="/review">← Back to queue</Link></div><div className="detail-grid"><div className="soft-panel"><div className="document-visual" /><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginTop:20}}><div><h3>{doc.verdict}</h3><p className="muted" style={{fontSize:12}}>Visual evidence layer · ELA analysis</p></div><div className="risk-number">{doc.score}<small>risk score</small></div></div></div><div className="soft-panel"><div className={`badge ${doc.tone}`}>{doc.verdict}</div><h2 style={{marginTop:16}}>Signal summary</h2><p className="muted" style={{fontSize:14,lineHeight:1.6}}>The combined reading prioritises this document for human review. Inspect the highlighted regions alongside the semantic consistency result before recording a decision.</p><div className="detail-list"><div><span>ELA score</span><strong>{doc.score > 50 ? "88.2" : "5.2"}</strong></div><div><span>Metadata</span><strong>{doc.score > 50 ? "Tampered" : "Clear"}</strong></div><div><span>Semantic audit</span><strong>{doc.score > 50 ? "Discrepancy" : "Passed"}</strong></div><div><span>Processing time</span><strong>1.2 sec</strong></div></div><div className="actions"><button className="button primary">Mark verified</button><button className="button secondary">Request review</button></div></div></div></>}
