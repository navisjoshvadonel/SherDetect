import Link from "next/link";
import { documents } from "../data";
export default function DocumentsPage(){return <><div className="page-heading"><div><div className="eyebrow">Document library</div><h1>Every file has a trail.</h1></div><p>Searchable evidence, clear status, no dashboard clutter. Select a document to move from signal to decision.</p></div><div className="actions" style={{marginBottom:24}}><input placeholder="Search by document name or ID" style={{maxWidth:340}} /><select style={{maxWidth:180}} defaultValue="all"><option value="all">All domains</option><option>Finance</option><option>Identity</option><option>HR</option></select><Link className="button primary" href="/submit">+ New audit</Link></div><div className="soft-panel table-panel"><div className="table-head"><span>Document</span><span>Type</span><span>Submitted</span><span>Verdict</span></div>{documents.map(doc=><Link className="table-row" href={`/review?doc=${doc.id}`} key={doc.id}><div><span className="doc-name">{doc.name}</span><span className="doc-id">{doc.id} · {doc.domain}</span></div><span>{doc.type}</span><span>{doc.date}</span><span className={`badge ${doc.tone}`}>{doc.verdict}</span></Link>)}</div></>}
