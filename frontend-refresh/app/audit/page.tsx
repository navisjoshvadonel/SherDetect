import { documents } from "../data";
export default function AuditPage(){return <><div className="page-heading"><div><div className="eyebrow">Audit trail</div><h1>A record you can return to.</h1></div><p>Immutable-looking clarity for every submission, analysis and review action in the workspace.</p></div><div className="soft-panel table-panel"><div className="table-head"><span>Event</span><span>Actor</span><span>Time</span><span>State</span></div>{documents.map((doc,index)=><div className="table-row" key={doc.id}><div><span className="doc-name">{index===0?"Forensic report generated":"Document submitted"}</span><span className="doc-id">{doc.id} · {doc.name}</span></div><span>{index===0?"SherDetect AI":"Alex Taylor"}</span><span>{doc.date}</span><span className={`badge ${doc.tone}`}>{doc.verdict}</span></div>)}</div></>}
